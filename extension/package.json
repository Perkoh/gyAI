{
  "name": "gyai-extension",
  "version": "1.0.0",
  "description": "gyAI browser extension — real-time, notification-only domain intelligence (Manifest V3).",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "build:dev": "webpack --mode development",
    "dev": "webpack --mode development --watch",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "copy-webpack-plugin": "^12.0.2",
    "rimraf": "^5.0.10",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
